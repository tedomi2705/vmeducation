class Tour {
  bool isExpanded;
  final int tourId;
  final String title;
  final String content;

  Tour({
    required this.isExpanded,
    required this.tourId,
    required this.title,
    required this.content,
  });
}